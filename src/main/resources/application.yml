spring:
  security:
    user:
      password: Test
  application:
    name: "Patron Mode"
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

# App Properties
coz:
  app:
    jwtSecret: cozSecretkey
    jwtExpirationMs: 86400000

